{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run FastAPI Server",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "cwd": "${workspaceFolder}",
      "args": [
        "api.app:app",
        "--reload"
      ],
      "console": "integratedTerminal"
    }
  ]
}
